# XL103 — check-not-passing

**Severity:** WARN  
**Confidence:** CERTAIN  
**Requires:** CACHED_VALUES

A `chk_*` defined name evaluates to something other than `0` or `TRUE`.

**Remediation:** correct the model until the check evaluates to 0 or TRUE.

**Fixture:** `chk_total_tie` cached at `1450`. Forced backend failure must SKIP,
not report clean.
